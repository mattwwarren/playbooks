[project]
name = "personal-playbooks"
version = "0.1.0"
description = "Ansible project for laptop management."
requires-python = ">=3.13"
dependencies = [
    "ansible",
    "ansible-lint",
    "jmespath",
]
